name = "aavegotchi-petter"
main = "src/index.ts"
compatibility_date = "2024-12-01"

# Cron: every 12 hours at 00:00 and 12:00 UTC
[triggers]
crons = ["0 0,12 * * *"]

# No [vars] here. Use `npm run deploy` (runs with --keep-vars) so Dashboard vars are preserved.
# Set these once in Cloudflare Dashboard → Variables and Secrets:
#   BASE_RPC_URL, DASHBOARD_URL, WALLET_ADDRESS, PRIVATE_KEY, REPORT_SECRET
